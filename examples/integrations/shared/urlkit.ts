// Repository-local bridge used by the integration examples.
//
// A published application should import from `@cookbook/urlkit` directly.
// These examples import the repository source so they can run from this
// checkout before the package has been built and published.
export * from '../../../src/index.js';
